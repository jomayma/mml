# encoding: UTF-8
require 'spec_helper'

describe BooksController do
  describe "searching books" do
    it "should call the model method that performs book search"
    it "should select the Search Results template for rendering"
    it "should make the book search results available to that template"
  end
end
