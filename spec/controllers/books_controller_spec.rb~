require 'spec_helper'

describe BooksController do
  describe 'searching books on Google books' do
    it 'should call the model method that performs google book search' do
      pending 'Need to choose a name for model method'
    end
    it 'should select the Search Results template for rendering' do
      flunk 'No template exists yet'
    end
    it 'should make the google book search results available to that template'
  end
end
